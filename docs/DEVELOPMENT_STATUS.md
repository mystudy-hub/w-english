# 工程实现状态

日期：2026-09-12。按 [IMPLEMENTATION_SPEC.md](./IMPLEMENTATION_SPEC.md) 推进的 Stage 0～2 工程已覆盖 Stage 2 预览范围，并完成本轮逐项核对与缺口修正。工程测试与正式发布验收分别记录；完整目标仍需要真实素材和人工、真机、发布环境证据，见 [规格核对记录](./qa/SPEC_AUDIT_2026-09-12.md)。

用户已明确“先完成工程，语音素材随后提供”。当前不合成替代教学语音，不填写人工审批，也不把测试信号放入应用包。

## 已有工程

| 范围 | 当前产物 | 证据与边界 |
|---|---|---|
| 工程基线 | Vite 8、React 18.3.1、TS 6、Tailwind 4、精确 lockfile；Node 24.19.0 / npm 11.17.0 | 已验证干净安装、类型检查、lint 和预览构建；远端 CI 未运行 |
| Stage 2 内容 | 100 个不同词、100 张 SVG、动物之家 34 词 / 阳光花园 33 词 / 快乐学校 33 词 | 89 个拼读词、11 个整词识别词；原 20 词 ID 和顺序保留；全部 draft |
| 内容与许可 | 共用 Zod 契约、词义/图片指纹、图形许可台账及随包许可文件 | 自有图形及 Lucide、Nunito 使用记录和文件哈希可验证；图义、文本、发音仍待老师审核 |
| 场景与解锁 | 三场景入口、按场景下载、公共依赖复用、前置场景 5 个不同词首次答对后解锁 | 未解锁场景不预取媒体；直达路由不能绕过资格或写探索记录；下载失败不收回资格 |
| 卡片与听音选图 | 五词分页、常速/慢速/例句、亲子提示、3/4/4 选项、L3 有效计时、复习选题 | 目标自然播完才开放选择；重试、超时、提示、首次答对与 24 小时复习分别记录 |
| 拼读与拼字母 | 按实际音素区分气泡、整词拼合、点选/拖拽及键盘替代、一词练习或最多五词一轮 | 重复字母分别定位；双写辅音只播一次；静音字母无伪造声音；摆放和待听片段可恢复；跳过不发星 |
| 高频词与奖励 | 高频词例句整词高亮；每题参与星；每 5 个不同词的有效首次答对获一枚贴纸 | 拼字母不写词汇首次/复习答对；贴纸与听音事务一并提交，支持旧进度补发去重；图鉴暂为静态 |
| 休息与家长设置 | 8 / 12 / 18 分钟设置；前台计时；结束当前题后提醒；12 分钟一次延长；18 分钟休息 3 分钟 | 家长页、后台和横屏提示暂停；刷新保留累计与截止时间；休息时停止后续题音、拼读和闲置向导 |
| 本地记录 | Dexie schema 2（原生版本 20），八张表，事务去重、题组恢复、导出与确认清除 | 已知 v1 七表原样迁移；未知版本/结构拒绝升级且可只读备份；清除失败完整回滚；迟到查询、退出和休息返回均受使用与题组版本保护，退出失败可重试 |
| 历史保留 | 90 天作答/结束题组清理，保留当前题组、近期明细及最近 20 道已结束听音题所需记录 | 聚合进度和贴纸长期保留；家长清除保留设置、休息状态与已下载内容 |
| 音频与向导 | 独立语音/SFX 通道、12 个学习向导与独立横屏提示、取消、上下文重建和设备切换缓冲 | 旧学习包保留壳提示兜底；横屏提示模态、焦点循环、家长入口与模拟安全区通过；真实录音/设备待验收 |
| 儿童可读性 | 两种模式的最小触控区/间距、20px学习正文、文字与关键反馈图标对比度、安全区布局 | 960×600代表页面实测；亲子面板插图不遮挡按钮；tap隐藏菜单标签并保留可访问名称 |
| 单窗口写入 | 从进入到返回欢迎页持有 Web Lock；交接等待已受理事务并重新读取设置、进度和会话 | 卡片、家长、题组、完成页与双窗口交接已测；取消的开始请求不能借用下一次使用的权限创建隐藏题组；不支持 Web Locks 时仅卡片体验 |
| 离线与更新 | 哈希复核、每场景暂停/继续、原会话固定清单、完整旧版回退、共享文件保留、核心预缓存筛选 | 真实SW/Range/跨版本/子路径通过；可变索引不使用immutable；旧核心文件保留但不重复预缓存；线上响应待核 |
| 音频生产与审核 | 分批打包、无损PCM中间文件及哈希、慢速/响度/峰值、编码后解码、原子索引与审核页 | 真工具验证PCM格式、时长/越界/静音反例和损坏文件；审批绑定文案/IPA/速度、源/最终文件和切片 |
| 发布准备 | preview/release 门禁、CI 配置、静态托管响应头、等待 SW 更新 | release 按预期拒绝当前草稿；未提交、推送、部署或把预览当正式版本 |

## 当前验证

环境：Windows、Node 24.19.0、npm 11.17.0、Vitest 5.0.0、Playwright 1.63.0 / Chromium 153。本地浏览器测试使用 2 个 worker，CI 使用 1 个。

- **163 项单元/规则/事务/音频工具测试、44 项浏览器测试通过**；横屏文字最后调整后追加该场景复核。覆盖原竞态/恢复及本轮可读性、安全区、PCM/解码、缓存和迁移失败重试。
- 类型检查、lint、100 词目录检查、预览构建、109 个素材文件的哈希/许可检查通过。
- 预览应用壳只预缓存当前核心资源，17 个条目约 **693.3 KiB**；为原始文件总大小，并非真机压缩传输测量。
- 当前内容版本 **2026.09.3**；**353 个语音片段**待提供：300 个学习语音、40 个音素、12 个学习向导及 1 个横屏提示。100 个慢速词派生，实际输入 **253 个 WAV**。内置 SFX 不计入语音。
- `audio:inventory` 与 `content:review` 已按当前 100 词重新生成；正式审批台账仍为空。
- 详细证据与测试限制见 [工程验证记录](./qa/ENGINEERING_2026-09-12.md)；`artifacts/qa/engineering-2026-09-12.json` 记录当前250个工程源文件指纹和已观察的检查结果。

## 交付前仍需完成

1. 后续提供真实整词、例句、音素和向导录音及来源许可；可分批接入，方法见 [AUDIO_WORKFLOW.md](./AUDIO_WORKFLOW.md)。
2. 老师逐词审校 100 词文字、图义、拼读拆分和最终语音切片，并通过既有审核工具导入。脚本通过不能代替人工批准。
3. iPad/Android 真机、Safari/iPadOS 16.4 与 Chrome 111 最低目标、设备声音中断/切换、安装与实际性能验收。
4. 至少 3 名儿童的使用观察，以及真正间隔 24 小时的复习证据。自动时钟测试只证明边界规则。
5. 经授权取得 HTTPS 预览/部署环境并运行远端 CI，使用正式素材通过 release 门禁后完成发布验收。

Stage 3 的录音、晚安、自动调级、喂食、丰富动画和更多词汇，仍按规格等待使用反馈与资源条件，不在当前预览中展示未生效设置。

## 后续接入入口

- `npm run dev`：本地工程预览。
- `npm run audio:inventory`：全量录音清单；也支持 `-- --theme animal_home` 或 `-- --sprite guide`。
- `npm run audio:pack -- --theme animal_home`：在对应真实 WAV 到位后分批打包。
- `npm run content:review`：生成 [本地审核页](../artifacts/review/index.html)，供老师核对和导出结果。

当前 `data/content-approvals.json`、`data/audio-approvals.json` 为空；100 词均为 draft，尚不具备正式发布资格。
